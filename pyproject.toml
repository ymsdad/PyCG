[project]
name = "cppcg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    # Use modern py-tree-sitter API
    "tree-sitter>=0.21",
    "pytest>=7.0.0",
]

[tool.pytest.ini_options]
testpaths = ["test"]
